var React = require('react');

var Header = React.createClass({
  render: function() {
    return (
      <header className="header">
        <span className="logo">RITA</span>
      </header>
    );
  }
});

module.exports = Header;
